# 🎨 Инструкция по генерации текстур Pudge Wars

## Быстрый старт

### Способ 1: Автоматическая генерация (рекомендуется)

1. **Откройте генератор:**
   - Дважды кликните на `texture_generator.html`
   - Или откройте через браузер: `file:///path/to/texture_generator.html`

2. **Сгенерируйте текстуры:**
   - Нажмите кнопку **"Generate All Textures"**
   - Дождитесь завершения (1-2 секунды)

3. **Скачайте текстуры:**
   - Нажмите **"Download All"** (скачаются 3 файла)
   - Или скачайте по одной через ссылки под каждым canvas

4. **Переместите в папку assets/:**
   ```
   ground_texture.png → assets/
   water_texture.png → assets/
   wall_texture.png → assets/
   ```

5. **Запустите игру:**
   ```bash
   node server.js
   ```

6. **Проверьте:**
   - Откройте `http://localhost:8080`
   - Дождитесь "✓ Assets ready"
   - Нажмите "Start Game"

---

### Способ 2: Ручная генерация (если есть Photoshop/GIMP)

Создайте текстуры самостоятельно:

#### Ground Texture (земля)
- **Размер:** 512x512 (или кратно 64)
- **Цвета:** #3a2e2e (основа), #4a5a3a (трава), #6a6a6a (камни)
- **Элементы:** трещины, сухая трава, камни, мох

#### Water Texture (вода)
- **Размер:** 512x512
- **Цвета:** #1a3a4a (глубина), #2a4a5a (волны), #648ca0 (блики)
- **Элементы:** волны, рябь, пузырьки, отражения

#### Wall Texture (стена)
- **Размер:** 512x512
- **Цвета:** #504a45 (камни), #3a3a3a (раствор), #4a5a3a (мох)
- **Элементы:** каменные блоки 64x64, трещины, мох, выветривание

---

## 🎨 Стиль Warcraft 3 Pudge Wars

### Референсы:
- **Земля:** потрескавшаяся грязь, болотная почва, сухая трава
- **Вода:** мутная, зелёно-синяя, с волнами и отражениями
- **Стены:** старые каменные блоки, покрытые мхом

### Цветовая палитра:
```
Ground:  #3a2e2e, #4a3a2a, #4a5a3a, #6a6a6a
Water:   #1a3a4a, #2a4a5a, #3a5a6a, #648ca0
Wall:    #504a45, #3a3a3a, #4a5a3a, #2a2a2a
```

---

## ❓ Troubleshooting

### Текстуры не загружаются
- Проверьте, что файлы в папке `assets/`
- Проверьте консоль на ошибки 404
- Убедитесь, что имена файлов точные (lowercase)

### Игра использует fallback (простые цвета)
- Это нормально! Игра работает и без текстур
- Для лучшей графики — сгенерируйте текстуры

### Генератор не открывается
- Проверьте, что файл `texture_generator.html` существует
- Откройте через другой браузер (Chrome, Firefox)

---

## 🚀 Деплой на Render

После генерации текстур:

```bash
git add assets/*.png
git commit -m "Add Pudge Wars textures"
git push origin main
```

Render автоматически задеплоит текстуры через 1-2 минуты.

---

## 📊 Характеристики текстур

| Текстура | Размер | Формат | Стиль |
|----------|--------|--------|-------|
| Ground   | 512x512 | PNG    | Потрескавшаяся грязь с травой |
| Water    | 512x512 | PNG    | Мутная болотная вода |
| Wall     | 512x512 | PNG    | Каменные блоки со мхом |

> 💡 Текстуры тайловые — они повторяются без видимых швов.
