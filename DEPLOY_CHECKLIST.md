# ✅ Чеклист перед деплоем на Render

## Файлы
- [x] `index.html` — клиентская часть
- [x] `server.js` — сервер + раздача статики
- [x] `js/map_renderer.js` — рендерер карты
- [x] `assets/*.svg` — текстуры (3 файла)
- [x] `render.yaml` — конфиг для Render
- [x] `package.json` — зависимости
- [x] `yarn.lock` — lock-файл Yarn (генерируется при деплое)
- [x] `.gitignore` — игнорирование node_modules, package-lock.json
- [x] `.nvmrc` — версия Node.js (18)

> ✅ Проект использует **Yarn** для совместимости с Render.  
> `package-lock.json` удалён и игнорируется.

## Проверка локально
```bash
node server.js
```
Открыть `http://localhost:8080`

### Ожидания:
1. ✅ Видно стартовый экран с "Loading assets..."
2. ✅ Прогресс загрузки: ✓ ground_texture, ✓ water_texture, ✓ wall_texture
3. ✅ Кнопка "Start Game" становится активной
4. ✅ Индикатор онлайна: "🟢 Server connected"
5. ✅ После старта — карта с текстурами, рекой, стенами

## Проверка на Render

### 1. Деплой
```bash
git add .
git commit -m "Deploy to Render"
git push origin main
```

### 2. После деплоя
1. Открыть URL вида `https://pudge-wars-xxxx.onrender.com`
2. Открыть консоль (F12)
3. Проверить логи:
   ```
   [ASSETS] Loaded: ground (assets/ground_texture.svg)
   [ASSETS] Loaded: water (assets/water_texture.svg)
   [ASSETS] Loaded: wall (assets/wall_texture.svg)
   [ASSETS] All textures loaded.
   ```

### 3. Мультиплеер
1. Открыть URL в двух вкладках
2. На обеих вкладках нажать "Start Game"
2. Проверить, что индикатор онлайна показывает "🟢 2 players online"
3. Игроки видят друг друга на карте

## ⚠️ Если что-то не работает

### "Loading assets..." бесконечно
- Проверить консоль на ошибки 404
- Проверить, что файлы `assets/*.svg` существуют на сервере
- Проверить MIME-типы в `server.js`

### Кнопка не активируется
- Проверить, что все 3 текстуры загрузились
- Через 5 секунд кнопка активируется автоматически (fallback)

### Мультиплеер не работает
- Проверить WebSocket соединение (консоль → Network → WS)
- Убедиться, что оба игрока на одном URL
- Проверить логи сервера на Render

---

## 🎮 Готово!

Если все пункты ✅ — игра готова к продакшену!
