# 🎨 Texture Generator - Руководство

## Быстрый старт

### Автоматическая генерация (рекомендуется)

```bash
# При установке зависимостей (автоматически)
npm install

# Или вручную
npm run generate-textures
```

Текстуры будут сгенерированы в папке `assets/`:
- `ground_texture.png` — земля (512x512)
- `water_texture.png` — вода (512x512)
- `wall_texture.png` — стена (512x512)

---

## Что генерируется?

### Ground Texture (Земля)
**Стиль:** Потрескавшаяся болотная почва Warcraft 3

**Элементы:**
- 🟤 Тёмная грязевая основа (#3a2e2e)
- 🌿 Пятна сухой травы (#4a5a3a)
- 🪨 Мелкие камни (#6a6a6a)
- 🌱 Пятна мха (#4a5a3a, прозрачность 40%)
-  cracks Трещины в земле

**Особенности:**
- Бесшовная текстура (tileable)
- 5000+ слоёв шума для детализации
- 30 случайных трещин
- 200 пятен травы
- 100 камней с бликами

---

### Water Texture (Вода)
**Стиль:** Мутная болотная река из Pudge Wars

**Элементы:**
- 🌊 Градиент глубины (#1a3a4a → #2a4a5a)
- 〰️ Волны (синусоидальные паттерны)
- 💫 Рябь (круги)
- 🫧 Пузыри/пена (#648ca0)
- ✨ Блики на поверхности

**Особенности:**
- Анимированная (смещение в игре)
- 50 паттернов ряби
- 300 пузырей
- 100 поверхностных бликов
- Глубинные вариации

---

### Wall Texture (Стена)
**Стиль:** Старые каменные стены арены

**Элементы:**
- 🧱 Каменные блоки 64x64
- 🕳️ Раствор между блоками (#3a3a3a)
- 🌿 Мох на краях (#4a5a3a)
- 💔 Трещины в камнях
- 🌧️ Выветривание

**Особенности:**
- Кирпичная кладка со смещением
- 50 текстурных точек на блок
- Случайный мох (50% вероятность)
- 200 пятен выветривания

---

## Технические детали

### Требования
- Node.js >= 18.0.0
- canvas ^2.11.2 (автоматически устанавливается)

### Размер текстур
- **Исходный:** 512x512 пикселей
- **В игре:** Масштабируется до 64x64
- **Формат:** PNG (без потерь)

### Бесшовность (Tileable)
Все текстуры бесшовные — они повторяются без видимых границ.

**Алгоритм:**
1. Генерация базового слоя
2. Копирование краёв
3. Alpha blending границ (64px)
4. Слияние углов

---

## Настройка

### Изменение размера
В `generate_textures.js`:
```javascript
const TEXTURE_SIZE = 512; // Измените на 1024 для HD
```

### Изменение цветовой палитры
В функциях генерации:
```javascript
// Ground colors
ctx.fillStyle = '#3a2e2e'; // Основной цвет
ctx.fillStyle = '#4a5a3a'; // Трава
ctx.fillStyle = '#6a6a6a'; // Камни
```

### Добавление деталей
Добавьте больше элементов в функции:
```javascript
// Пример: больше травы
for (let i = 0; i < 500; i++) { // Было 200
  // ...
}
```

---

## Устранение проблем

### Ошибка при установке canvas
```bash
# Windows
npm install --global --production windows-build-tools
npm install canvas

# Linux
sudo apt-get install libcairo2-dev libjpeg-dev libpango1.0-dev giflib-tools

# macOS
brew install pkg-config cairo pango libpng jpeg giflib
```

### Текстуры не генерируются
1. Проверьте, что `canvas` установлен:
   ```bash
   npm list canvas
   ```
2. Запустите вручную:
   ```bash
   npm run generate-textures
   ```

### Текстуры не отображаются в игре
1. Проверьте консоль на ошибки 404
2. Убедитесь, что файлы в `assets/`
3. Перезапустите сервер:
   ```bash
   npm restart
   ```

---

## Примеры использования

### В игре (клиент)
```javascript
// Загрузка текстур
assetManager.preload().then(() => {
  console.log('Textures loaded!');
  startGame();
});

// Отрисовка земли
const groundTex = assetManager.get('ground');
for (let tx = 0; tx < tilesX; tx++) {
  for (let ty = 0; ty < tilesY; ty++) {
    ctx.drawImage(groundTex, x, y, 64, 64);
  }
}
```

### Кастомная генерация
```javascript
// Сгенерировать только землю
const { generateGroundTexture } = require('./generate_textures.js');
generateGroundTexture();
```

---

## Лицензия

MIT — используйте в своих проектах!

---

## 🎮 Готово!

Теперь у вас есть качественные текстуры в стиле **Warcraft 3 Pudge Wars**!

Для деплоя на Render:
```bash
git add assets/*.png
git commit -m "Add generated textures"
git push origin main
```
