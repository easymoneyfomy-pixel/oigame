# Contributing to Pudge Wars

Спасибо за интерес к проекту Pudge Wars! Этот документ поможет вам начать работу.

## 🚀 Быстрый старт

```bash
# Клонирование репозитория
git clone <repository-url>
cd gam_oi

# Установка зависимостей
npm install

# Запуск в режиме разработки
npm run dev

# Запуск тестов
npm test
```

## 📁 Структура проекта

```
gam_oi/
├── index.html          # Клиент (HTML5 Canvas + JS)
├── server.js           # Сервер (Node.js + WebSocket)
├── package.json        # Зависимости и скрипты
├── test/
│   └── server.test.js  # Тесты сервера
└── assets/             # Текстуры и ресурсы
```

## 🎯 Как внести изменения

### 1. Создайте ветку

```bash
git checkout -b feature/your-feature-name
```

### 2. Внесите изменения

- Следуйте существующему стилю кода
- Добавляйте комментарии к сложным функциям
- Пишите тесты для новых функций

### 3. Запустите тесты

```bash
npm test
```

### 4. Закоммитьте изменения

```bash
git add .
git commit -m "Add: your feature description"
git push origin feature/your-feature-name
```

### 5. Создайте Pull Request

- Опишите ваши изменения
- Укажите связанные issue (если есть)
- Дождитесь code review

## 📝 Стиль кода

### JavaScript

- Используйте `const` и `let` (избегайте `var`)
- Стрелочные функции для callback'ов
- Именуйте функции понятно и описательно

```javascript
// ✅ Хорошо
const playerHealth = calculateHealth(player);
const isDead = (health) => health <= 0;

// ❌ Плохо
const h = player.health;
const d = (h) => h <= 0;
```

### Комментарии

- Объясняйте **почему**, а не **что**
- Добавляйте JSDoc для сложных функций

```javascript
/**
 * Вычисляет урон от крюка с учётом брони
 * @param {number} baseDamage - базовый урон
 * @param {number} armor - броня цели
 * @returns {number} фактический урон
 */
function calculateDamage(baseDamage, armor) {
  const reduction = armor / (100 + armor);
  return baseDamage * (1 - reduction);
}
```

## 🧪 Тестирование

### Запуск тестов

```bash
npm test
```

### Добавление тестов

Добавляйте тесты в `test/server.test.js`:

```javascript
test('должна вычислять расстояние', () => {
  const a = { x: 0, y: 0 };
  const b = { x: 3, y: 4 };
  assert.strictEqual(distance(a, b), 5);
});
```

## 🐛 Отчёт об ошибках

Создайте issue с меткой "bug" и укажите:

- Описание проблемы
- Шаги для воспроизведения
- Ожидаемое поведение
- Скриншоты (если применимо)

## 💡 Предложения функций

Создайте issue с меткой "enhancement" и опишите:

- Какую проблему решает функция
- Как она должна работать
- Примеры использования

## 🔧 Технические требования

- Node.js >= 18.0.0
- npm или yarn
- Современный браузер (Chrome, Firefox, Edge)

## 📚 Ресурсы

- [WebSocket API](https://developer.mozilla.org/en-US/docs/Web/API/WebSocket)
- [HTML5 Canvas](https://developer.mozilla.org/en-US/docs/Web/API/Canvas_API)
- [Node.js Documentation](https://nodejs.org/docs)

## 🎮 Игровая документация

- [README.md](README.md) - общая информация
- [CHANGELOG.md](CHANGELOG.md) - история изменений

## 📞 Контакты

- GitHub Issues - для вопросов по коду
- Discord - для общения с сообществом

Спасибо за ваш вклад! 🎉
