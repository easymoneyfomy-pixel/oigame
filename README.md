# 🥩 PUDGE WARS

Многопользовательская браузерная .io игра — клон Pudge Wars из Warcraft 3.

## 🌐 Деплой на Render

### 1. Подготовка
Убедитесь, что у вас есть:
- Аккаунт на [Render.com](https://render.com)
- Git-репозиторий с игрой на GitHub/GitLab

### 2. Развёртывание
1. Зайдите в [Render Dashboard](https://dashboard.render.com)
2. Нажмите **New +** → **Web Service**
3. Подключите ваш репозиторий
4. Render автоматически обнаружит `render.yaml` и применит настройки
5. Нажмите **Create Web Service**

### 3. Проверка
- После деплоя вы получите URL вида: `https://pudge-wars-xxxx.onrender.com`
- Откройте его в браузере
- Дождитесь **"✓ Assets ready"** и нажмите **Start Game**

### 4. Мультиплеер
- Откройте URL в нескольких браузерах/вкладках
- Все игроки подключаются к одному URL

---

## 🚀 Быстрый старт (локально)

### 1. Запуск сервера
```bash
node server.js
```

### 2. Подключение
Откройте в браузере: `http://localhost:8080`

### 3. Ожидание загрузки
- Дождитесь сообщения **"✓ Assets ready"** на стартовом экране
- Кнопка **Start Game** станет активной (зелёной)
- Введите имя (или оставьте пустым — будет присвоено автоматически)
- Нажмите **Start Game**

### 4. Мультиплеер
- Откройте `http://localhost:8080` в **нескольких браузерах/вкладках**
- Каждый игрок видит свой стартовый экран
- Когда все игроки нажмут **Start Game** — начнётся матч

---

## 🎮 Управление

| Клавиша | Действие |
|---------|----------|
| `W` `A` `S` `D` | Перемещение |
| `Мышь` | Прицеливание |
| `ЛКМ` | Выстрел крюком |
| `🔊` | Вкл/выкл звук |
| `1-4` | Купить апгрейд |

## 🎯 Механики

### Meat Hook (Крюк)
- Выстрел крюком в направлении курсора
- **Попадание во врага** → урон + притягивание + визуальный эффект
- **Попадание в союзника** → спасение (притягивание без урона)
- **Столкновение хуков** → отскок в противоположные стороны

### Система прокачки 💎
Убивая врагов, вы получаете **золото** (150 за убийство, 50 за ассист).

| Апгрейд | Макс уровень | Эффект за уровень | Стоимость |
|---------|--------------|-------------------|-----------|
| 📏 **Range** | 5 | +50 дальности | 100 → 150 → 225... |
| ⚡ **Speed** | 5 | +2 скорости полёта | 150 → 225 → 337... |
| 💀 **Damage** | 5 | +50 урона | 200 → 300 → 450... |
| ⏱️ **Cooldown** | 5 | -300мс кулдауна | 250 → 375 → 562... |

**Базовые характеристики:**
- Дальность: 400
- Скорость: 15
- Урон: 300
- Кулдаун: 3000мс (3 сек)


## 📊 Производительность

- **60 TPS** серверный тикрейт
- **60 FPS** клиентский рендеринг
- **Оптимизация**: отправка только изменений
- **Масштабирование**: до 10 игроков на сервере


## 🎨 Визуальная стилизация карты (Pudge Wars Style)

Карта теперь полностью переработана под эстетику оригинальной Pudge Wars из Warcraft 3:

### ✅ Особенности
- **Изометрическая проекция** с тайловым рендерингом
- **Потрескавшаяся грязь** — текстура земли (`assets/ground_texture.png`)
- **Мутная река** с анимацией волн (`assets/water_texture.png` + горизонтальное смещение + мерцание)
- **Каменные стены** по краям арены (`assets/wall_texture.png`)
- **Береговые границы** — чёткая разница между землёй и водой
- **Виньетка** — затемнение по краям экрана для мрачной атмосферы
- **Предзагрузка ассетов** — игра не запускается до полной загрузки текстур

### 📁 Структура ассетов
```
assets/
├── ground_texture.svg   # Земля (64x64, потрескавшаяся грязь)
├── water_texture.svg    # Вода (64x64, мутная с волнами)
└── wall_texture.svg     # Стена (64x64, каменная)
```

> 💡 Используются SVG вместо PNG для меньшего размера и лучшего качества.

### ⚙️ Оптимизация
- `ctx.imageSmoothingEnabled = false` для чёткости тайлов
- Тайловая отрисовка без перерисовки всей карты
- Анимация воды — только смещение текстуры (0 overhead)
- Виньетка — один `createRadialGradient()` на кадр

### 🧪 Проверка FPS
В консоли браузера выводится текущий FPS каждую секунду:
```
[FPS] 60
[FPS] 59
...
```
Цель: стабильные **60 FPS** даже при 10 игроках.


## 🔧 Troubleshooting

### Кнопка Start Game не активируется
1. Откройте консоль браузера (F12)
2. Проверьте наличие ошибок загрузки ассетов
3. Если видите `[ASSETS] Failed to load` — проверьте наличие файлов в `assets/`
4. Через 5 секунд кнопка активируется автоматически (fallback режим)

### Игра запускается, но карта не отображается
1. Проверьте, что `js/map_renderer.js` загружен (нет 404 ошибки)
2. Проверьте консоль на наличие `[MAP] Assets loaded`
3. Перезагрузите страницу (Ctrl+R)

### Мультиплеер не работает
1. Убедитесь, что сервер запущен: `node server.js`
2. Проверьте, что все игроки подключились к одному адресу
3. Откройте консоль — должно быть `Connected` в статусе

---

## ⚠️ Важно для Render (бесплатный тариф)

- **Сервер «засыпает»** через 15 минут бездействия
- При первом запросе после «сна» — **холодный старт** (~30-50 секунд)
- Для продления активности:
  - Используйте [UptimeRobot](https://uptimerobot.com) (пинг каждые 5 минут)
  - Или перейдите на платный тариф Render

---

## 📁 Структура проекта
```
pudge-wars/
├── index.html           # Клиент (HTML + CSS + JS)
├── server.js            # Сервер (WebSocket + HTTP)
├── render.yaml          # Конфиг для Render
├── package.json         # Зависимости Node.js
├── .gitignore           # Игнорируемые файлы
├── assets/              # Текстуры (SVG)
│   ├── ground_texture.svg
│   ├── water_texture.svg
│   └── wall_texture.svg
└── js/
    └── map_renderer.js  # Рендерер карты
```
